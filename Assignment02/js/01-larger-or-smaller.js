let num1 = parseInt(window.prompt('Please enter a number:'));
let num2 = parseInt(window.prompt('Please enter a second number:'));

if (num1 >= num2) {
  document.write(num1); 
} else {
  document.write(num2);
}